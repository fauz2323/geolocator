import 'package:flutter/material.dart';

class SplashTittle extends StatelessWidget {
  const SplashTittle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Geolocator"),
    );
  }
}
